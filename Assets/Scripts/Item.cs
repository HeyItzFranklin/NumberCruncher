using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item : MonoBehaviour
{
    //initialize variables here: need an array for colors
    //we'll also eventually need a way to read the player's number for the spawn range

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //need something in here to check the player's number
        //need something in here to delete the item after it is picked up
        // do we want to set player values here or in the other script?


    }
}
